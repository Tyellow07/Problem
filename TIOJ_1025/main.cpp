#include <bits/stdc++.h>
#define IOS ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
#define endl "\n"
using namespace std;

int a[10][10];

int check(int x,int y,int c)
{
    for(int i=1;i<=9;i++)
    {
        
    }
}

int dfs(int x,int y)
{
    if(a[x][y]==0)
    {
        for(int i=1;i<=9;i++)
        {
            if(check(x, y,i);
        }
    }
}

int main()
{
    IOS
    for(int i=1;i<=9;i++)
    {
        for(int k=1;i<=9;k++)
        {
            cin >> a[i][k];
        }
    }
    dfs(1,1)
}
